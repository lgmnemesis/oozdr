import { ChatTimeUniquePipe } from './chat-time-unique.pipe';

describe('ChatTimeUniquePipe', () => {
  it('create an instance', () => {
    const pipe = new ChatTimeUniquePipe();
    expect(pipe).toBeTruthy();
  });
});
